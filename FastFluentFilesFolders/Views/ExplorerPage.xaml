<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="FastFluentFilesFolders.Views.ExplorerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FastFluentFilesFolders.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:FastFluentFilesFolders.Views"
    xmlns:models="using:FastFluentFilesFolders.Models"
    xmlns:CTKTCtrls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="7*" />
        </Grid.ColumnDefinitions>

        <TabView x:Name="TabsCtrl"
                 Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3"
                 TabItemsSource="{Binding Tabs}"
                 IsAddTabButtonVisible="True"
                 CanReorderTabs="True"
                 SelectionChanged="OnTabsSelectionChanged"
                 TabCloseRequested="OnTabsCloseRequested"
                 AddTabButtonClick="OnTabsAddTabClick"
                 Margin="8,4,8,0">
            <TabView.TabItemTemplate>
                <DataTemplate x:DataType="models:ExplorerTab">
					<TabViewItem Header="{Binding Title}" IsClosable="True">
					    <TabViewItem.IconSource>
						    <ImageIconSource ImageSource="{Binding FolderNode.Icon}" />
					    </TabViewItem.IconSource>
					</TabViewItem>
				</DataTemplate>
            </TabView.TabItemTemplate>
        </TabView>

        <views:TopView Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3"/>

        <ScrollViewer Grid.Row="2" Grid.Column="0"
                      VerticalScrollBarVisibility="Auto"
                      VerticalScrollMode="Auto">
            <StackPanel Orientation="Vertical">
                <views:PinnedShortcuts />
                <views:FileTreeView />
            </StackPanel>
        </ScrollViewer>

        <CTKTCtrls:GridSplitter Grid.Row="2" Grid.Column="1" Grid.RowSpan="1"/>

        <Grid Grid.Row="2" Grid.Column="2">
            <views:MiddleFilesView x:Name="MiddleFilesPanel" />
            <views:SettingsView x:Name="SettingsPanelView" x:Load="False" Visibility="Collapsed" />
        </Grid>
    </Grid>
</Page>